"""TextPreprocessor (design.md §1.2) — làm sạch văn bản trước khi chunking.

Không nằm trong các function signature chi tiết của design.md; coi là
backlog mở rộng (stretch goal) — bổ sung khi có nhu cầu thực nghiệm cụ thể
(ví dụ: loại bỏ whitespace thừa, header/footer lặp lại trong PDF).
"""
